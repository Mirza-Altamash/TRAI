const mongoose = require('mongoose'); const bcrypt = require('bcryptjs'); mongoose.connect('mongodb://127.0.0.1:27017/trai_citizen_hub').then(async () => { const hash = await bcrypt.hash('12345', 10); await mongoose.connection.collection('employees').updateMany({}, { $set: { passwordHash: hash, mustChangePassword: true } }); console.log('Passwords reset successfully'); }).catch(e => console.log(e)).finally(() => process.exit(0));
